// Extend Jest's expectations
expect.extend({
    // Add any custom matchers here
  });
  
  // Global setup code
  beforeAll(() => {
    // Add any global setup if needed
  });
  
  // Global teardown code
  afterAll(() => {
    // Add any global teardown if needed
  });